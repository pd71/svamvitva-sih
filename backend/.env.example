# Render / Cloud Environment Variables (Optional Defaults)

# Database Connection (Defaults to local SQLite file svamitva_drone.db)
DATABASE_URL=sqlite:///./svamitva_drone.db

# Python Version for Render Runtime
PYTHON_VERSION=3.11.0
